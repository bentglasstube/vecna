<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:vecna="http://schemas.android.com/apk/res-auto">
  <item android:id="@+id/search"
    android:icon="@drawable/ic_menu_search"
    vecna:showAsAction="always|withText"
    vecna:actionViewClass="android.support.v7.widget.SearchView"
    android:title="@string/menu_search" />
  <item android:id="@+id/lock"
    android:icon="@drawable/ic_menu_lock"
    vecna:showAsAction="ifRoom|withText"
    android:title="@string/menu_lock" />
  <item android:id="@+id/refresh"
    vecna:showAsAction="never"
    android:title="@string/menu_refresh" />
  <item android:id="@+id/settings"
    vecna:showAsAction="never"
    android:title="@string/menu_settings" />
  <item android:id="@+id/help"
    vecna:showAsAction="never"
    android:title="@string/menu_help" />
</menu>
